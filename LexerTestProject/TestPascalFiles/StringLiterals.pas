program Hello;
var str, str2 : string; // Comment
    c : char;
begin {comment
another comment}

  c :=    'a';
  str := 'A tabulator character: ??? is easy to embed and its not integer';
  str2 := 'begin';
  sstr := 'er
  ror';
  writeln(str);
end.