program nilDemo;
begin
	// point to nothing
	loc := nil;
	// clears dynamic array
	chk := nil;

    b:= not a;
	b:= not b and true; 
	b := not ((10 > a) or (a > 5));	
	//ava := +1 - -1;
	ava := +1 - 1;
end.